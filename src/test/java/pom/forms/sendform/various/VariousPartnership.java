package pom.forms.sendform.various;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

public class VariousPartnership extends VariousBase {

    // == constructors ==
    public VariousPartnership(WebDriver driver, WebElement details) {
        super(driver, details);
    }
}

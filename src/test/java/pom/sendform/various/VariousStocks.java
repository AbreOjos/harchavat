package pom.sendform.various;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

public class VariousStocks extends VariousBase {

    // == constructors ==
    public VariousStocks(WebDriver driver, WebElement details) {
        super(driver, details);
    }
}

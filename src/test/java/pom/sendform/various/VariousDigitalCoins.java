package pom.sendform.various;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

public class VariousDigitalCoins extends VariousBase {

    // == constructors ==
    public VariousDigitalCoins(WebDriver driver, WebElement details) {
        super(driver, details);
    }
}

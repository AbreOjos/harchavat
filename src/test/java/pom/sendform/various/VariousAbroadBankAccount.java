package pom.sendform.various;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

public class VariousAbroadBankAccount extends VariousBase {

    // == constructors ==
    public VariousAbroadBankAccount(WebDriver driver, WebElement details) {
        super(driver, details);
    }
}

package pom.sendform.various;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

public class  VariousNonWorkIncomes extends VariousBase {

    // == constructors ==
    public VariousNonWorkIncomes(WebDriver driver, WebElement details) {
        super(driver, details);
    }
}
